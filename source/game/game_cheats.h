#pragma once

// TODO: Temporary header; will move those 
// globals to the right place later
static bool gbUnlockAllRoads = false;

static bool gEnableMinimapDensity = true;
static bool gEnableMinimapAI = true;
static bool gEnableMinimapNetPlayers = true;
